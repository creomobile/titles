name: titles
description: High customizable title decorators for widgets and editors with common context data.
version: 0.0.4
homepage: https://github.com/creomobile/titles

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.8.0

flutter:
